customDetectors:
  - name: "Potential Password"
    regex: '(?i)(password|passwd|pwd)[\'"\s:=]+[^\s\'"]{4,}'
    keywords:
      - password
      - passwd
      - pwd
    pathRegex: '.*'
    entropy: 0.0

